namespace Arrays
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Single-Dimensional_Arrays
            //Array Declaration

            //Implictly Typed Array
            string[] words = { "House", "Road", "Mouse", "Sleep", "Chicken" };

            //Declaration by element type
            int[] scores = new int[10]; 
            scores[0] = 0;
            scores[1] = 1;
            scores[2] = 2;
            #endregion

        }
    }
}